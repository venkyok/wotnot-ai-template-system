# Render-compatible requirements (pre-compiled wheels only)
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-dotenv==1.0.1
pydantic==2.7.2
httpx==0.27.0
aiosqlite==0.20.0
sqlalchemy==2.0.31
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
PyJWT==2.8.0

# Use psycopg2-binary instead of psycopg2 (pre-compiled)
psycopg2-binary==2.9.9

# Remove problematic Rust-dependent packages
# orjson==3.10.7  # Requires Rust - use json instead
# watchfiles==0.23.0  # Requires Rust - uvicorn will use default

# Core dependencies for AI functionality
# openai  # Will be installed separately if needed

# Other essential packages
requests==2.32.3
starlette==0.37.2
typing_extensions==4.12.2

# Async database support
# asyncpg  # Only if using PostgreSQL

# Optional packages (comment out if causing issues)
# redis==4.6.0
# dramatiq
# apscheduler
# pandas
# phonenumbers

# Basic Python packages
six==1.16.0
urllib3==2.2.2
certifi==2024.7.4